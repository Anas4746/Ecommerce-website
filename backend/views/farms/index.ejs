<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shops</title>
</head>
<body>
    <h1>Shops Name</h1>
    <% for (let farm of farms) { %>
        <ul>
            <li><a href="farms/<%= farm._id %>"><%= farm.name %></a></li>
        </ul>
        <% } %>
        <a href="/farms/new">Add a Shop</a>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shops</title>
<style>
    
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 70%;
            max-width: 500px;
            display: flex;
            flex-direction: column;
            align-items: flex-start; /* Align children to the left */
        }
        .top-bar {
            display: flex;
            justify-content: space-evenly;
            align-items: center;
            width: 100%;
            margin-bottom: 10px;
            background-color: #006699;
        }
        .top-links a {
            text-decoration: none;
            color: #006699;
            margin-left: 10px;
        }
        .top-links a:hover {
            text-decoration: underline;
        }
        h1 {
            text-align: center;
            color: #ffffff;
            margin-bottom: 20px;
        }
        ul {
            list-style-type: none;
            padding-left: 20px;
            display: flex;
            flex-direction: row;
        }
        li {
            margin-bottom: 10px;
        }
        .add-shop-btn {
            background-color: #5cb85c;
            color: white;
            border: none;
            padding: 10px 15px;
            cursor: pointer;
            border-radius: 5px;
            width: 500%;
            text-align: center;
            display: block;
            margin-top: 20px;
        }
        .add-shop-btn:hover {
            background-color: #4cae4c;
        }
        a button{
            text-decoration: none;
        }
        a:hover{
            text-decoration: underline;
        }
        .top-links {
            display: flex;
            align-items: center;
            
        }
        .top-links a button {
            margin-left: 0px;
            background-color: #4cae4c;
            border-radius: 5px;
            border: none;
            color: white;
            font-size: 18px;
            padding: 7px;
        }
        #signout{
            background-color: goldenrod;
        }
        #login{
            background-color: goldenrod;
            font-size: large;
            
        }
        #owner-tag button{
            background-color: red;
            color:black;
            border-radius: 5px;
            border: transparent;
            font-size: x-small;
        }
        #farm_img {
            width: 100px; /* Adjust the width as needed */
            height: 100px; /* Maintain aspect ratio */
            margin-bottom: 10px;
        }
        .farm-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 10px;
            margin: 10px; /* Adjust spacing between items */
            /* width: calc(33.33% - 20px); Adjust the width for three items in a row */
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        #farm {
            text-align: center;
            text-decoration: none;
            color: black;
            margin: 0 10px ;
            font-size: larger;
        }

        .farm-row {
            display: flex;
            justify-content: space-evenly;
            flex-wrap: wrap;
        }
        #owner-tag {
            background-color: red;
            color: black;
            border-radius: 5px;
            border: transparent;
            font-size: x-small;
            padding: 2px 8px;
            margin-top: 5px;
        }
    
</style>
</head>
<body>
    <div class="container">
        <div class="top-bar">
            <h1>Shops Name</h1>
            <div class="top-links">
                <a href="/register"><button>Register</button></a>
                <% if (!islogin) { %>
                    <a href="/login"><button id="login">Login</button></a>
                <% } else { %>
                    <form action="/logout" method="post">
                        <a><button id="signout">Sign Out</button></a>
                    </form>
                <% } %>
            </div>
        </div>
                <ul class="farm-row">
            <% for (let index = 0; index < farms.length; index++) { %>
            <li>    
                <div class="farm-container">
                    <a id="farm" href="farms/<%= farms[index]._id %>">
                        <img id="farm_img" src="http://localhost:5000/<%= farms[index].image %>">
                        <br><%= farms[index].name %></a>
                    <% if (islogin == farms[index].users) { %>
                        <sub id="owner-tag"><button>Owner</button></sub>
                    <% } %>
                </div>
            </li>
            <% } %>
        </ul>
        <a href="/farms/new" style="color: white;"><button class="add-shop-btn">Add a Shop</button></a>
    </div>
</body>
</html>

