<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>
        <li>Price: $<%= product.price %></li>
        <li>Category: <%= product.category %></li>
        <li> <a href="/farms/<%= product.farm._id %>">Shop: <%= product.farm.name %></a></li>
    </ul>
    <a href="/product/<%= product._id %>/edit">Edit product</a>
    <form action="/product/<%= product._id %>?_method=DELETE" method="POST">
        <button>Delete Product</button>
    </form>
    
    <a href="/farms/<%= product.farm._id %>">See this Shop</a>
</body>
</html>
 -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 70%;
            max-width: 500px;
        }
        h1 {
            text-align: center;
            color: #006699;
            margin-bottom: 15px;
        }
        ul {
            list-style: none;
            padding: 0;
        }
        li {
            margin-bottom: 10px;
            color: #333;
        }
        a {
            text-decoration: none;
            color: #006699;
            font-size: 16px;
            margin-top: 10px;
        }
        a:hover {
            text-decoration: underline;
        }
        button {
            background-color: #d9534f; /* Red Color */
            color: white;
            border: none;
            padding: 10px 15px;
            cursor: pointer;
            border-radius: 5px;
            width: 100%;
            text-align: center;
            font-size: 16px;
            margin-top: 20px;
        }
        button:hover {
            background-color: #c9302c;
        }
        .edit-button {
            background-color: #006699; /* Blue Color */
        }
        .edit-button:hover {
            background-color: #005688;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= product.name %></h1>
        <ul>
            <li>Price: $<%= product.price %></li>
            <li>Category: <%= product.category %></li>
            <li><a href="/farms/<%= product.farm._id %>">Shop: <%= product.farm.name %></a></li>
        </ul>
        <% if(isOwner) {%>
        <a href="/product/<%= product._id %>/edit" style="color: white;"><button class="edit-button">Edit product</button></a>
        <form action="/product/<%= product._id %>?_method=DELETE" method="POST">
            <button>Delete Product</button>
        </form>
        <% } %>
        <a href="/farms/<%= product.farm._id %>">Go Back to Shop</a>
    </div>
</body>
</html>
